# Support des fichiers .knwf dans Info Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre la lecture des métadonnées depuis les archives ZIP KNIME.

---

### Task 1: Environnement et Utilitaires

- [ ] **Step 1: Installer adm-zip**
Run: `npm install adm-zip`
Run: `npm install -D @types/adm-zip`

- [ ] **Step 2: Créer src/utils/knime-parser.ts**
Implémenter `parseWorkflowMetadata(xmlData: string)` qui retourne les infos et les nœuds.

### Task 2: Intégration ZIP dans Info

- [ ] **Step 1: Mettre à jour src/commands/info.ts**
Gérer le cas `workflowArg === '-workflowFile'` en utilisant `adm-zip`.

- [ ] **Step 2: Générer un échantillon de test .knwf**
Créer `samples/wf_simple.knwf` à partir du dossier existant.

### Task 3: Validation

- [ ] **Step 1: Compiler le projet**
Run: `npm run build`

- [ ] **Step 2: Tester la commande sur le ZIP**
Run: `knime info -w samples/wf_simple.knwf`
